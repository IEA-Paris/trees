type Mailing {
  appId: String
  name: String
  description: String
  createdAt: String
  updatedAt: String
  date: AWSDateTime
  score: Float
}

type MailingList {
  total: Int!
  items: [Mailing]
}
