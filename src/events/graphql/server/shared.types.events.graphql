type Event {
  appId: String
  availableSlots: Int
  bookingState: bookingState
  category: eventCategories
  createdBy: ID
  dateText: String
  delay: AWSDateTime
  description: String
  details: String
  eventType: eventType
  image: Image
  location: Location
  name: String!
  outside: Boolean
  program: String
  related: Related
  score: Float
  slots: [Slot]
  slug: string
  start: AWSDateTime
  state: eventState
  stop: AWSDateTime
  subtitle: String
  summary: String
  totalSlots: Int
  url: AWSURL
}

type EventFilters {
  disciplines: [String!]
  fellowships: [String!]
  tags: [String!]
  category: [eventCategories!]
}

type EventsList {
  total: Int!
  items: [Event]
}
