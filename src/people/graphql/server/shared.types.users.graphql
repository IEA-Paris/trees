type User implements People {
  admin: Boolean
  apps: [AppRole]
  createdAt: AWSDateTime!
  email: [AWSEmail]!
  settings: Settings
  status: Int
  role: Int
  slug: AWSJSON
}

type UserList {
  total: Int!
  items: [User!]!
}
