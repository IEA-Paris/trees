type User {
  admin: Boolean
  apps: [AppRole]
  createdAt: AWSDateTime!
  email: [AWSEmail]!
  settings: Settings
  status: Int
  role: Int
  slug: string
}

type UserList {
  total: Int!
  items: [User!]!
}
