type Actions {
  appId: String
  color: String
  link: String
  image: Image
  name: String
  video: AWSURL
  slots: [String]
  slug: String
  score: Float
  start: AWSDateTime
  stop: AWSDateTime
}

type ActionList {
  total: Int!
  items: [Actions]
}
